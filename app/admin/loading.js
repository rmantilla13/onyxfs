import SectionLoading from './_ui/SectionLoading';

/** Any section of the panel while its data loads; the Overview's shape. */
export default function Loading() {
  return <SectionLoading tiles={4} rows={3} />;
}
